name: 'Install Python dependencies'
runs:
  using: "composite"
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        enable-cache: true
    - run: uv sync
      shell: bash
